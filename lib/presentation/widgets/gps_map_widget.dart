import 'package:flutter/material.dart';

class GPSMapWidget extends StatelessWidget {
  const GPSMapWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 300,
      color: Colors.grey[300],
      child: Center(
        child: Text('Map Placeholder'),
      ),
    );
  }
}
